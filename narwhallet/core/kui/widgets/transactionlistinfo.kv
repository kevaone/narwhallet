#:import _tr narwhallet.core.kui._translate

<TransactionListInfo>
    sm: None
    block: 'block'
    transaction: 'transaction'
    txvalue: 'txvalue'
    status: 'status'
    size_hint_y: None
    height: dp(85) if root.width <= dp(650) else dp(60)

    canvas:
        Color:
            rgba: self.hover_color if self.mouse_hover else self.background_color

        Rectangle:
            pos: self.pos
            size: self.size

    BoxLayout:
        orientation: 'vertical'

        # BoxLayout:
        #     orientation: 'horizontal'
        #     size_hint_y: None
        #     height: 10
        BoxLayout:
            orientation: 'horizontal'

            Nwlabel:
                # id: transaction
                text: root.transaction
                text_size: self.size
                padding: [5, 0, 0, 0]
                font_size: dp(17)
                size_hint_y: None
                height: dp(60) if root.width <= dp(650) else dp(30)

        BoxLayout:
            orientation: 'horizontal'
            size_hint_y: None
            height: dp(30)

            Nwlabel:
                # id: block
                text: root.block
                text_size: self.size
                padding: [5, 0, 0, 0]
            Nwlabel:
                # id: status
                text: root.status
                padding: [5, 0, 0, 0]
                text_size: self.size
            Nwlabel:
                text: _tr.translate('Value:')
                text_size: self.size
                halign: 'right'
                padding: [5, 0, 0, 0]
            Nwlabel:
                # id: txvalue
                text: root.txvalue
                halign: 'right'
                padding: [5, 0, 0, 0]
                text_size: self.size

        # BoxLayout:
        #     orientation: 'horizontal'
        #     size_hint_y: None
        #     height: 10
