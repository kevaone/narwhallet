<WalletListInfo>
    size_hint_y: None
    height: dp(75)
    sm: None
    ticker: ticker
    canvas:
        Color:
            rgba: self.hover_color if self.mouse_hover else self.background_color

        Rectangle:
            pos: self.pos
            size: self.size

    BoxLayout:
        orientation: 'vertical'

        BoxLayout:
            orientation: 'horizontal'
            size_hint_y: None
            height: dp(35)

            Nwimage:
                id: ticker
                source: 'narwhallet/core/kui/assets/keva-logo.png'
                height: dp(30)
                width: dp(45)

            Nwlabel:
                _text: root.wallet_name
                height: dp(35)
                font_size: dp(25)

        BoxLayout:
            orientation: 'horizontal'
            size_hint_y: None
            height: dp(20)

            Nwlabel:
                _text: 'Balance:'
                text_size: self.size
                halign: 'right'
                padding: [5, 0, 0, 0]

            Nwlabel:
                _text: root.balance
                halign: 'right'
                padding: [5, 0, 20, 0]
                text_size: self.size

        BoxLayout:
            orientation: 'horizontal'
            size_hint_y: None
            height: dp(20)

            Nwlabel:
                _text: 'Last Updated:'
                halign: 'right'
                padding: [5, 0, 0, 0]
                text_size: self.size

            Nwlabel:
                _text: root.last_updated
                halign: 'right'
                padding: [5, 0, 20, 0]
                text_size: self.size
