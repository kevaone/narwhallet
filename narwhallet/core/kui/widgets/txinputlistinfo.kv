<TXInputListInfo>
    orientation: 'vertical'
    sm: None
    txid: txid
    vout: vout
    sequence: sequence
    scriptsig: scriptsig
    # height: dp(85) if root.width < dp(550) else dp(65)
    size_hint_y: None
    height: self.minimum_height

    canvas:
        Color:
            rgba: (25/255, 27/255, 27/255, 1)

        Rectangle:
            pos: self.pos
            size: self.size

    BoxLayout:
        orientation: 'horizontal'
        size_hint_y: None
        height: dp(20)

        Nwlabel:
            _text: 'txid:'
            text_size: self.size
            padding: [10, 0, 0, 0]
            size_hint_x: None
            width: dp(50)

    BoxLayout:
        orientation: 'horizontal'
        size_hint_y: None
        height: self.minimum_height # dp(45) if root.width < dp(550) else dp(25)

        Nwlabel:
            id: txid
            _text: 'txid'
            valign: 'center'
            # text_size: self.size
            padding: [10, 5, 10, 5]
            # size_hint_y: None
            # height: dp(40) if root.width < dp(550) else dp(25)

            size_hint: 1, None
            text_size: self.width, None
            height: self.texture_size[1]

    BoxLayout:
        size_hint_y: None
        height: dp(20)
        orientation: 'horizontal'

        Nwlabel:
            _text: 'vout:'
            text_size: self.size
            halign: 'left'
            padding: [10, 0, 0, 0]
            size_hint_x: None
            width: dp(50)

        Nwlabel:
            id: vout
            _text: 'vout'
            text_size: self.size
            size_hint_x: None
            width: dp(50)

    BoxLayout:
        orientation: 'horizontal'
        size_hint_y: None
        height: dp(20)

        Nwlabel:
            _text: 'scriptsig:'
            text_size: self.size
            padding: [10, 0, 0, 0]
            size_hint_x: None
            width: dp(75)

    BoxLayout:
        orientation: 'horizontal'
        size_hint_y: None
        height: self.minimum_height # dp(45) if root.width < dp(550) else dp(25)

        Nwlabel:
            id: scriptsig
            _text: 'scriptsig'
            valign: 'center'
            # text_size: self.size
            padding: [10, 5, 10, 5]
            # size_hint_y: None
            # height: dp(40) if root.width < dp(550) else dp(25)

            size_hint: 1, None
            text_size: self.width, None
            height: self.texture_size[1]

    BoxLayout:
        size_hint_y: None
        height: dp(5)
        orientation: 'horizontal'

    BoxLayout:
        size_hint_y: None
        height: dp(20)
        orientation: 'horizontal'

        Nwlabel:
            _text: 'sequence:'
            text_size: self.size
            halign: 'left'
            padding: [10, 0, 0, 0]
            size_hint_x: None
            width: dp(100)

        Nwlabel:
            id: sequence
            _text: 'sequence'
            text_size: self.size

    BoxLayout:
        orientation: 'vertical'
        size_hint_y: None
        height: self.minimum_height
